#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <iomanip>
using namespace std;

void decipher(const unsigned long *const inBuf, unsigned long *const outBuf, const unsigned long * const key);
